import styled from 'styled-components/native';

export const ContainerTextBlank = styled.View`
    width: ${props => props.width}px;
    height: ${props => props.height / 2}px;
    align-items: center;
    justify-content: center;
`;
