import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    containerImageUser: {
        position: 'absolute',
        top: 16,
        right: 20,
    },
    imageUser: {
        height: 44,
        width: 44,
        borderRadius: 50,
    },
});
